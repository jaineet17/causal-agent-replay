# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Local data / generated artifacts
data/*.db
data/traces/
*.html.tmp

# Caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local operational notes (personal publishing handoffs) — not project docs
PUBLISH_HANDOFF.md
ARXIV_SUBMISSION.md

# Who&When benchmark cache + results (dataset has no published license — never commit)
data/whowhen/
